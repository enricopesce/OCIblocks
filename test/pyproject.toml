[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool]
[tool.poetry]
package-mode = false
[tool.poetry.dependencies]
pulumi = "3.122.0"
pulumi_oci = "2.0.0"
python = "^3.8"
random-word = "1.0.11"
pyright = "^1.1.373"
